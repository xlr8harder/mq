[project]
name = "mq"
version = "0.2.7"
description = "Command-line tool to query configured LLM chat models"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "llm-client @ git+https://github.com/xlr8harder/llm_client.git@f663628644d5291a5520c19245ee69ae9c805ea5",
]

[project.scripts]
mq = "mq.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
